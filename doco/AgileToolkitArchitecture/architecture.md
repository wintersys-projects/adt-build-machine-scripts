1. When you begin a build with this buildkit it will expect you to have selected a build chain in the file:

   ${BUILD_HOME}/builddescriptors/buildstyles.dat

   If you have selected the "standard" build
